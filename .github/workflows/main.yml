name: Build LogScope (Auto-Engine)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout LogScope Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.2'

      # 🔥 STEP 1: BLACKBOX ENGINE MANUFACTURING
      # हम 'JavaNoober' का असली कोड लाकर खुद अपना इंजन बनाएंगे
      - name: Build BlackBox Engine from Source
        run: |
          echo "1. Creating Isolated Factory Directory..."
          cd $RUNNER_TEMP
          
          echo "2. Cloning JavaNoober BlackBox Repo..."
          # रिपोर्ट के अनुसार JavaNoober ही असली सोर्स है
          git clone https://github.com/JavaNoober/BlackBox.git blackbox_src
          cd blackbox_src
          
          echo "3. Building the Core Engine (Bcore)..."
          chmod +x gradlew
          # यह कमांड असली .aar फाइल जनरेट करेगा
          ./gradlew :Bcore:assembleRelease
          
          echo "4. Installing Engine into LogScope..."
          # हम बनी हुई फाइल को अपने प्रोजेक्ट में कॉपी कर रहे हैं
          mkdir -p $GITHUB_WORKSPACE/app/libs
          cp Bcore/build/outputs/aar/*.aar $GITHUB_WORKSPACE/app/libs/blackbox-core.aar
          
          echo "✅ Engine Successfully Manufactured & Installed!"

      # 🔥 STEP 2: BUILD LOGSCOPE APP
      - name: Grant execute permission for LogScope gradlew
        run: chmod +x gradlew

      - name: Build Release APK
        run: ./gradlew assembleRelease --stacktrace --no-daemon

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: logscope-release
          path: app/build/outputs/apk/release/*.apk
